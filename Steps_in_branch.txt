Задача ветки: получение, нормализация в ECS, хранение.

• (+)	Пока я хочу начать с того, что разверну в докер компоузе брокера (kafka, обязательно конкретную стейбл версию, чтобы зафиксировать) и бд postgreSQL (аналогично с фиксацией). Брокер и бд должны быть доступны как на локалхосте так и снаружи для агентов извне. 
• (+)	Затем я разверну агента Fluent Bit на хосте сервера (локально по сути), проверить чтение активных и статичных логов
• (+)	Свяжу агента с брокером и проверю, что все приходит и разделяется по источникам (1 источник = 1 топик кафки), приходит json {timestamp (время обработки лога), log (сырой лог)}
•	    Затем создам базовые таблицы для приложения (самого бэкенда (пользователи, нормализаторы, источники и прочее)) и для хранения нормализованных логов (та же бд, только другая схема, чтобы отделить приложение от логов) 
•	    После этого нужно будет создать базовый бэкенд для этой бд, чтобы писать правила нормализатора пользователем из веб интерфейса, отлаживать их на окне просмотра и сопостовлять с источником если все хорошо и ложить в бд в таком случае